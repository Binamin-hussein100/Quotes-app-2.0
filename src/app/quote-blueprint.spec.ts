import { QuoteBlueprint } from './quote-blueprint';
export { QuoteBlueprint}
describe('QuoteBlueprint', () => {
  it('should create an instance', () => {
    expect(new QuoteBlueprint()).toBeTruthy();
  });
});
